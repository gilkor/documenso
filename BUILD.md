To build use this command
```shell
DOCKER_REPOSITORY="asia-southeast1-docker.pkg.dev/gilkor-elys/opensources/documenso" ./docker/buildx.sh
```
it will create image with 2 tags, the current commit id and the `latest`.